#pragma once

namespace vem {
// Say we have two spaces F and G such that G \subset F.
// As input we take a matrix M in block form
// [ A B ]
// [ C D ]
// where the first block represents coefficients in F and second represents coefficients in G.
// a ProjectorConstructor constructs a M-adjoint projection into
struct ProjectorConstructor {
};
}// namespace vem
